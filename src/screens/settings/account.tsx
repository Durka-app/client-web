import { FC } from 'react';
import { Typography } from '@mui/material';

type Props = Record<string, never>;

export const AccountCategory: FC<Props> = () => {
  return <Typography>You have no account</Typography>;
};
